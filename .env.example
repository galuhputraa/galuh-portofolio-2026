# Copy to .env.local for local dev, and set the same key in Vercel
# (Project -> Settings -> Environment Variables) for Production.
#
# Absolute origin of the deployed site. Without it, OpenGraph and Twitter image
# URLs fall back to http://localhost:3000 and social previews break.
# No trailing slash.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
